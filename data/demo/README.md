# Authored teaching dataset

These 25 pages are synthetic fixtures with fictional revision IDs. Their prose
is authored for the UI and tests; it is not copied Wikipedia evidence and must
not be displayed as such. All synthetic entity IDs are in a separate namespace.
Regenerate with `python scripts/make_fixtures.py` from the repository root.
Real revision snapshots are collected separately under `data/wikipedia`.
