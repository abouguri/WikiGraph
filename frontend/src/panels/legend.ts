import {state} from '../store';
import {button,element} from './detail';
export function renderLegend(container:HTMLElement,change:()=>void){container.replaceChildren();const types=[...new Set([...state.nodes.values()].map(n=>n.type||'Other'))].sort();if(state.colorBy==='cluster'){const clusters=[...new Set([...state.nodes.values()].map(n=>n.cluster||0))].sort((a,b)=>a-b);for(const c of clusters){const tag=element('span',`Cluster ${c+1}`,'legend-chip');tag.style.color=`var(--${['language','person','concept','organization','work','other'][c%6]})`;container.append(tag)}container.append(element('p','Clusters summarize similarity, not factual categories.','hint'))}if(state.colorBy==='year')container.append(element('p','Earlier → later','year-gradient'));for(const type of types){const key=type.toLowerCase();const b=button(type,()=>{state.types.has(key)?state.types.delete(key):state.types.add(key);change()},'legend-chip');b.setAttribute('aria-pressed',String(!state.types.has(key)));b.setAttribute('aria-label',`Show ${type} entities`);const dot=element('i');dot.style.background=`var(--${['language','person','concept','organization','work'].includes(key)?key:'other'})`;b.prepend(dot);container.append(b)}if(!types.length)container.append(element('p','Build a map to see its entity types.','hint'))}
