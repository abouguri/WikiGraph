import {state} from '../store';
import {button,element,relations} from './detail';
import type {GraphNode} from '../encode';
export function renderList(panel:HTMLElement,tab:string,sort:string,actions:{select:(id:string)=>void;add:(id:string)=>void;hover:(id:string)=>void;inspect:(id:string)=>void},hidden:Set<string>){panel.replaceChildren();if(tab==='connections'){const edges=[...state.edges.values()].sort((a,b)=>a.id.localeCompare(b.id));for(const [i,e]of edges.entries()){if(hidden.has(e.subject)||hidden.has(e.object)||(state.predicate!=='all'&&e.predicate!==state.predicate)||(state.direction!=='both'&&!state.origins.some(id=>state.direction==='out'?e.subject===id:e.object===id)))continue;const label=(id:string)=>state.nodes.get(id)?.label||id;const li=element('li');li.dataset.edge=e.id;if(state.path)li.append(element('span',`Step ${i+1}`,'step-number'));li.append(element('div',`${label(e.subject)} ${relations[e.predicate]||e.predicate} ${label(e.object)}`,'connection-route'),button('Inspect evidence',()=>actions.inspect(e.id)),button(`Select ${label(e.subject)}`,()=>actions.select(e.subject)),button(`Select ${label(e.object)}`,()=>actions.select(e.object)));panel.append(li)}}else{let nodes:GraphNode[]=tab==='saved'?[...state.saved.values()]:tab==='foundations'?state.lists.foundations:tab==='builds'?state.lists.builds_on_this:[...state.nodes.values()].filter(n=>!hidden.has(n.id));nodes=[...nodes].sort((a,b)=>sort==='name'?a.label.localeCompare(b.label):sort==='year'?(b.year||0)-(a.year||0):sort==='links'?(b.in_links||0)-(a.in_links||0):(b.similarity||0)-(a.similarity||0));for(const n of nodes){const li=element('li');li.dataset.node=n.id;li.classList.toggle('selected',n.id===state.selection);li.onpointerenter=()=>actions.hover(n.id);li.onpointerleave=()=>actions.hover('');li.append(button(n.label,()=>state.nodes.has(n.id)?actions.select(n.id):actions.add(n.id),'entity-row'));const count='linked_by'in n?`${n.linked_by} map connections`:n.is_origin?'Origin':`${Math.round((n.similarity||0)*100)}% similarity`;li.append(element('span',count,'row-meta'));if(!state.nodes.has(n.id))li.append(button('Add to map',()=>actions.add(n.id)));panel.append(li)}}if(!panel.childElementCount)panel.append(element('li',tab==='foundations'||tab==='builds'?'No entities meet the three-connection threshold after excluding corpus hubs.':tab==='saved'?'Save an entity from its details to keep it here.':'No matching entities or connections.','empty-list'))}
