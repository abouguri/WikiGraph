{
  "name": "wikigraph-explorer",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/main.ts --bundle --minify --outfile=../src/wikigraph/static/app.js",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "esbuild": "^0.25.0",
    "prettier": "^3.9.8",
    "typescript": "^5.8.0"
  }
}
